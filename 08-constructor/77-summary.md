- instance initializer

  - instance fields
  - static fields
  - `this`

- static constructor

  - static fields
  - NO instance fields
  - NO `this` keyword

- default constructor

  - if no constructor is created by C# compiler

- object initializer
  - some field only
